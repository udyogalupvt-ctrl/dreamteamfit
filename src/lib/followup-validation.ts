import { z } from "zod";import { FOLLOWUP_PRIORITIES,FOLLOWUP_SOURCES } from "@/types/models";
export const followUpSchema=z.object({clientId:z.string().optional(),inquiryId:z.string().nullable().optional(),clientNameSnapshot:z.string().trim().min(2,"Member name is required").max(120),phoneSnapshot:z.string().trim().min(7,"Phone is required").max(20),source:z.enum(FOLLOWUP_SOURCES),reason:z.string().trim().min(2,"Reason is required").max(160),notes:z.string().trim().max(2000),followUpDate:z.string().min(10,"Date is required"),followUpTime:z.string().min(4,"Time is required"),priority:z.enum(FOLLOWUP_PRIORITIES),assignedTo:z.string().trim().max(120),nextAction:z.string().trim().max(300),outcome:z.string().trim().max(120)});
