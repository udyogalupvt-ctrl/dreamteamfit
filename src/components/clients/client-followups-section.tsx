import { useState } from "react";import { CalendarClock,Plus } from "lucide-react";import { FollowUpDialog } from "@/components/followups/followup-dialog";import { EmptyState } from "@/components/common/empty-state";import { ErrorState } from "@/components/common/error-state";import { Shimmer } from "@/components/common/loading-state";import { StatusPill } from "@/components/common/status-pill";import { Button } from "@/components/ui/button";import { useLive } from "@/hooks/use-live-query";import { formatDateISO,todayISO } from "@/lib/format";import { subscribeClientFollowUps } from "@/services/followups.service";import type { Client,FollowUp } from "@/types/models";
export function ClientFollowUpsSection({client}:{client:Client}){const live=useLive<FollowUp[]>((ok,fail)=>subscribeClientFollowUps(client.id,ok,fail),[],[client.id]);const [open,setOpen]=useState(false),today=todayISO();if(live.loading)return <Shimmer className="h-40 rounded-xl"/>;if(live.error)return <ErrorState error={live.error} title="Couldn't load follow-ups"/>;const groups=[["Overdue",live.data.filter(x=>x.status==="pending"&&x.followUpDate<today)],["Upcoming",live.data.filter(x=>x.status==="pending"&&x.followUpDate>=today)],["Past",live.data.filter(x=>x.status!=="pending")]] as const;return <div className="space-y-4"><div className="flex justify-end"><Button onClick={()=>setOpen(true)}><Plus/> Create Follow-up</Button></div>{live.data.length===0?<EmptyState icon={CalendarClock} title="No follow-ups yet" description="Schedule the next conversation with this client."/>:groups.map(([title,items])=>items.length?<section className="surface-card p-5" key={title}><h2 className="text-section-title">{title} Follow-ups</h2><div className="mt-3 divide-y divide-border">{items.map(x=><article key={x.id} className="flex flex-col gap-2 py-3 sm:flex-row sm:items-center"><div className="min-w-0 flex-1"><p className="font-semibold">{formatDateISO(x.followUpDate)} · {x.followUpTime}</p><p className="text-meta">{x.outcome||x.reason} · {x.notes||"No notes"}</p><p className="text-meta">{x.nextAction||"No next action"} · {x.assignedTo||"Unassigned"}</p></div><StatusPill tone={x.status==="completed"?"success":x.followUpDate<today?"danger":"warning"}>{x.status==="pending"&&x.followUpDate<today?"overdue":x.status}</StatusPill></article>)}</div></section>:null)}<FollowUpDialog open={open} onOpenChange={setOpen} clients={[client]} initialClient={client}/></div>}
