import { useState } from "react";import { toast } from "sonner";import { Field,FormDialog } from "@/components/common/form-dialog";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";import { Textarea } from "@/components/ui/textarea";import { completeFollowUp } from "@/services/followups.service";import { firestoreErrorMessage } from "@/services/firestore.service";import { CALL_OUTCOMES,type FollowUp } from "@/types/models";
export function CompleteFollowUpDialog({item,onClose}:{item:FollowUp|null;onClose:()=>void}){const [outcome,setOutcome]=useState("Interested"),[other,setOther]=useState(""),[notes,setNotes]=useState(""),[nextAction,setNextAction]=useState(""),[date,setDate]=useState(""),[time,setTime]=useState("10:00"),[saving,setSaving]=useState(false);const save=async()=>{if(!item)return;setSaving(true);try{await completeFollowUp(item,{outcome:outcome==="Other"?(other||"Other"):outcome,notes, nextAction,...(date?{nextFollowUpDate:date,nextFollowUpTime:time}:{})});toast.success("Follow-up completed");onClose()}catch(e){toast.error(firestoreErrorMessage(e))}finally{setSaving(false)}};return <FormDialog open={Boolean(item)} onOpenChange={v=>!v&&onClose()} title="Record Call Outcome" description={item?`${item.clientNameSnapshot} · ${item.phoneSnapshot}`:""} footer={<><Button variant="outline" onClick={onClose}>Cancel</Button><Button disabled={saving} onClick={()=>void save()}>Save Follow-up</Button></>}><div className="grid gap-4"><Field label="Call outcome" htmlFor="outcome"><Select value={outcome} onValueChange={setOutcome}><SelectTrigger id="outcome"><SelectValue/></SelectTrigger><SelectContent>{CALL_OUTCOMES.map(x=><SelectItem key={x} value={x}>{x}</SelectItem>)}</SelectContent></Select></Field>{outcome==="Other"?<Field label="Other outcome" htmlFor="other"><Input id="other" value={other} onChange={e=>setOther(e.target.value)}/></Field>:null}<Field label="Notes" htmlFor="out-notes"><Textarea id="out-notes" value={notes} onChange={e=>setNotes(e.target.value)}/></Field><Field label="Next action" htmlFor="out-next"><Input id="out-next" value={nextAction} onChange={e=>setNextAction(e.target.value)}/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Next follow-up date" htmlFor="out-date"><Input id="out-date" type="date" value={date} onChange={e=>setDate(e.target.value)}/></Field><Field label="Time" htmlFor="out-time"><Input id="out-time" type="time" value={time} onChange={e=>setTime(e.target.value)}/></Field></div></div></FormDialog>}
