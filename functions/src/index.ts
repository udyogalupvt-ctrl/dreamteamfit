import {initializeApp} from "firebase-admin/app";import {getFirestore,FieldValue} from "firebase-admin/firestore";import {onSchedule} from "firebase-functions/v2/scheduler";
initializeApp();const db=getFirestore();const TZ="Asia/Kolkata";const date=(d=new Date())=>new Intl.DateTimeFormat("en-CA",{timeZone:TZ,year:"numeric",month:"2-digit",day:"2-digit"}).format(d);const plus=(iso:string,n:number)=>{const d=new Date(`${iso}T12:00:00Z`);d.setUTCDate(d.getUTCDate()+n);return d.toISOString().slice(0,10)};const id=(...p:string[])=>p.join("__").replace(/[^a-zA-Z0-9_-]/g,"_");const render=(t:string,v:Record<string,string>)=>t.replace(/{{\s*(\w+)\s*}}/g,(_,k:string)=>v[k]??"");
async function settings(){const s=await db.doc("settings/automation").get();return {automationEnabled:true,renewalEnabled:true,renewalDaysBefore:7,birthdayEnabled:true,renewalTemplate:"Hi {{name}} 👋\n\nYour Rebuild Fitness membership expires on {{expiryDate}}.\n\nYour membership has 7 days remaining.\n\nPlease renew your membership to continue your fitness journey without interruption.\n\n— REBUILD FITNESS",birthdayTemplate:"Happy Birthday {{name}}! 🎉\n\nEveryone at Rebuild Fitness wishes you a fantastic birthday.\n\nStay strong, stay healthy and keep moving forward!\n\n— REBUILD FITNESS",...s.data()}}
async function queue(kind:"renewal"|"birthday",key:string,c:FirebaseFirestore.DocumentData,message:string,extra:Record<string,unknown>){const specific=db.doc(`${kind==="renewal"?"renewalNotifications":"birthdayNotifications"}/${key}`);return db.runTransaction(async tx=>{if((await tx.get(specific)).exists)return false;const now=FieldValue.serverTimestamp(),common={clientId:c.id,clientNameSnapshot:c.fullName,phoneSnapshot:c.phone,message,status:"queued",provider:"mock",sentAt:null,createdAt:now,updatedAt:now};tx.set(specific,{...common,...extra});tx.set(db.doc(`notifications/${key}`),{...common,type:kind,referenceId:key,scheduledFor:date(),error:""});tx.set(db.doc(`automationActivities/${key}`),{type:kind==="renewal"?"renewal_queued":"birthday_queued",referenceId:key,clientId:c.id,clientNameSnapshot:c.fullName,description:kind==="renewal"?"Renewal reminder queued":"Birthday greeting queued",createdAt:now,updatedAt:now});return true})}
export async function processRenewalReminders(){const cfg=await settings();if(!cfg.automationEnabled||!cfg.renewalEnabled)return;const today=date(),target=plus(today,Number(cfg.renewalDaysBefore)),[members,clients]=await Promise.all([db.collection("memberships").where("status","==","active").get(),db.collection("clients").get()]),all=members.docs.map(x=>({id:x.id,...x.data()})),cm=new Map(clients.docs.map(x=>[x.id,{id:x.id,...x.data()}]));for(const m of all.filter(x=>x.endDate===target)){if(all.some(x=>x.clientId===m.clientId&&x.id!==m.id&&x.status!=="cancelled"&&x.endDate>m.endDate))continue;const c=cm.get(m.clientId);if(!c)continue;const key=id(m.id,"renewal_7_days",today);await queue("renewal",key,c,render(String(cfg.renewalTemplate),{name:String(c.fullName),expiryDate:m.endDate}),{membershipId:m.id,expiryDate:m.endDate,reminderDate:today,type:"renewal_7_days"})}}
export async function processBirthdayNotifications(){const cfg=await settings();if(!cfg.automationEnabled||!cfg.birthdayEnabled)return;const today=date(),year=Number(today.slice(0,4)),clients=await db.collection("clients").get();for(const d of clients.docs){const c={id:d.id,...d.data()};if(!c.dateOfBirth||String(c.dateOfBirth).slice(5)!==today.slice(5))continue;const key=id(c.id,String(year),"birthday");await queue("birthday",key,c,render(String(cfg.birthdayTemplate),{name:String(c.fullName)}),{birthdayDate:today,year,type:"birthday"})}}
export const dailyRetentionAutomation=onSchedule({schedule:"every day 08:00",timeZone:TZ},async()=>{await processRenewalReminders();await processBirthdayNotifications()});
